# 万维正式版 → 新版 DSH 同步审计

基准：正式版 `E:\code\dsh\deepseek-harness-master`（`ec7b123`）与新版分支 `new-version`。

同步原则：正式版产品行为与视觉为基准；新版 DSH 的 Cordis、Store、Remote、Lexical 等官方架构保留，通过万维适配层接入，不用近似组件替代正式版组件。

## 已完成并验证

- 品牌：正式版 `brand-mark.svg`、`brand-wordmark.svg` 的形状、尺寸和蓝色主色原样迁入。
- 首页：正式版品牌锁定比例、徽标位置、侧栏品牌高度和输入字号已恢复。
- 登录页：正式版布局几何、品牌字标、蓝色配色、短信 Mock 表单和可刷新二维码交互已恢复；登录状态仍走新版 OneAPI Controller。
- 账户设置：正式版账户卡片和退出按钮布局已恢复，同时保留新版返回的可用模型信息。
- 技能市场：正式版完整页面结构已迁回，包括“我的安装”“我的上传”、公开/私人/审核记录、审核筛选、审核表格、ZIP/目录导入和详情页。
- 技能安装：补回 ZIP 原生安装命令以及大小、条目数、路径穿越、符号链接校验。
- 成果展示：正式版空间分析成果卡片与样式已恢复，新版原生调用桥保留。
- 文件拖入：补回浏览器普通文件拖入和桌面原生拖入的遮罩、导入状态及错误反馈；图片继续走新版附件机制，普通文件进入工作区；支持文件夹递归导入、同名重命名、符号链接拒绝和大小/数量限制。
- 下载定位：补回 Windows/macOS/Linux 在文件管理器中定位 Session 导出文件的原生命令与权限。
- 用户提问：补回非中文模型问题/选项的中文兜底显示，同时保留新版 Session Store 草稿机制。
- 工具标题：补回正式版中文业务标题（如“读取文件 · Read”“运行命令 · Bash”）。
- 技能调用行：补回正式版“加载技能”“查看详情”文案。
- 静态资源：连接器图标 17/17、技能图标 10/10 与正式版哈希一致。
- 数据沿用：桌面端不再默认强制使用预览版独立 `dsh-home`，默认沿用已有 DSH 数据；需要隔离时可显式设置 `WANWEI_DSH_HOME`。

## 新版架构适配（不是视觉删减）

- 旧 `ClientContext/IWorkspaces` 已映射到新版 Cordis `Context` 与 `remote.directoryPicker`。
- 旧 textarea composer 已由新版 Lexical composer 替代；正式版外部尺寸、字号和产品操作保留，编辑器内部继续使用新版实现。
- 旧 Host description 文件打开能力已映射到新版 `workspacePathOpen` observable。
- OneAPI 登录状态继续使用新版 `AuthView` observable，不恢复旧版全局订阅器。

## 仍需最终运行态核验

- Windows 实机：登录页在 100%、125%、150% DPI 下的像素位置。
- Windows 实机：首页、侧栏、技能市场、设置页的视觉截图对照。
- 文件夹递归拖入、ZIP 技能导入、技能审核列表真实接口联调。
- macOS/Linux 的安装包视觉与文件管理器打开行为。

## 非运行时源素材差异

- 旧桌面图标目录中的 `wanwei-source.png`、`zjugis-mark-source.png` 是图标制作源稿，新版运行和打包使用的 `.ico`、`.icns`、PNG 尺寸文件均已具备；这两张源稿不影响页面、功能或安装包图标。

## 不迁移范围

- 新版 DSH 官方新增能力不删除。
- OneAPI 线上服务镜像及数据库不重建；客户端只复用既有接口和数据。
- OneAPI 服务端的管理员令牌复制、用户问题审计、技能审核和办公技能脚本继续由现有线上镜像提供，不重复拷入桌面客户端仓库。
- 正式版不存在的新版官方页面，不强行改造成旧版样式。
