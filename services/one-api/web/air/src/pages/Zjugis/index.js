import React, { useEffect, useMemo, useState } from 'react';
import { API } from '../../helpers';

const emptyChannel = { name: '', type: 1, key: '', base_url: '', models: '', groups: 'default', other: '', openai_organization: '', model_mapping: '', system_prompt: '', auto_ban: 1, priority: 0, weight: 1, test_model: '', status: 1 };
const channelTypes = [[1, 'OpenAI Compatible'], [17, '阿里云百炼'], [26, '智谱'], [14, 'Anthropic'], [25, 'Moonshot'], [15, '百度文心'], [16, 'ChatGLM'], [18, '讯飞星火'], [23, '腾讯混元'], [24, 'Google Gemini'], [22, 'FastGPT']];
const splitModels = (v) => String(v || '').split(/[\n,]/).map((x) => x.trim()).filter(Boolean);

function useList(path, query = '') {
  const [state, setState] = useState({ rows: [], loading: false, error: '' });
  const refresh = async () => { setState((s) => ({ ...s, loading: true, error: '' })); try { const res = await API.get(`${path}${query ? `?${query}` : ''}`); if (!res.data?.success) throw new Error(res.data?.message || '请求失败'); setState({ rows: Array.isArray(res.data.data) ? res.data.data : [], loading: false, error: '' }); } catch (e) { setState({ rows: [], loading: false, error: e?.message || '服务暂不可用' }); } };
  useEffect(() => { refresh(); }, [path, query]); return { ...state, refresh };
}
function PageHead({ kicker, title, description, action }) { return <div className="preview-page-head"><div><div className="preview-kicker">{kicker}</div><h1>{title}</h1><p>{description}</p></div>{action}</div>; }
function Modal({ title, children, onClose, wide = false }) { return <div className="zjugis-modal-backdrop" onMouseDown={(e) => e.target === e.currentTarget && onClose()}><div className={`zjugis-modal${wide ? ' wide' : ''}`}><div className="zjugis-modal-head"><h2>{title}</h2><button onClick={onClose} aria-label="关闭">×</button></div>{children}</div></div>; }
function Field({ label, ...props }) { return <label className="zjugis-field"><span>{label}</span><input {...props} /></label>; }
function SelectField({ label, value, onChange, children }) { return <label className="zjugis-field"><span>{label}</span><select value={value ?? ''} onChange={onChange}>{children}</select></label>; }

export function ModelConfigPage() {
  const list = useList('/api/channel/', 'p=0&page_size=100&id_sort=false'); const [editing, setEditing] = useState(null); const [tab, setTab] = useState('channels'); const [form, setForm] = useState(emptyChannel); const [busy, setBusy] = useState(''); const [probe, setProbe] = useState(null); const [definitions, setDefinitions] = useState([]); const [modelEdit, setModelEdit] = useState(null); const [options, setOptions] = useState({ PreConsumedQuota: '', DefaultContextLimit: '' }); const rows = list.rows;
  const models = useMemo(() => [...new Set(rows.flatMap((r) => splitModels(r.models)))], [rows]);
  const set = (key, value) => setForm((f) => ({ ...f, [key]: value }));
  const open = (row) => { setProbe(null); setEditing(row || {}); setForm(row ? { ...emptyChannel, ...row, models: row.models || '', groups: row.group || row.groups || 'default' } : { ...emptyChannel }); };
  const save = async (e) => { e.preventDefault(); let mapping = form.model_mapping || ''; if (mapping.trim()) { try { mapping = JSON.stringify(JSON.parse(mapping)); } catch { window.alert('模型映射必须是合法 JSON'); return; } } const payload = { ...form, models: splitModels(form.models).join(','), group: form.groups || 'default', model_mapping: mapping, auto_ban: Number(form.auto_ban) ? 1 : 0 }; delete payload.groups; const res = form.id ? await API.put('/api/channel/', { ...payload, id: form.id }) : await API.post('/api/channel/', payload); if (res.data?.success) { setEditing(null); list.refresh(); } else window.alert(res.data?.message || '保存失败'); };
  const action = async (row, name, value = '') => { if (name === 'delete' && !window.confirm(`确定删除渠道 ${row.name}？`)) return; setBusy(`${name}-${row.id}`); try { let res; if (name === 'delete') res = await API.delete(`/api/channel/${row.id}/`); else if (name === 'copy') res = await API.post(`/api/channel/copy/${row.id}`); else if (name === 'test') res = await API.get(`/api/channel/test/${row.id}?model=${encodeURIComponent(value || row.test_model || '')}`); else if (name === 'enable' || name === 'disable') res = await API.put('/api/channel/', { id: row.id, status: name === 'enable' ? 1 : 2 }); else res = await API.put('/api/channel/', { id: row.id, [name]: value }); if (!res.data?.success) throw new Error(res.data?.message || '操作失败'); if (name === 'test') window.alert(res.data.message || '渠道测试成功'); list.refresh(); } catch (e) { window.alert(e.message || '操作失败'); } finally { setBusy(''); } };
  const fetchModels = async () => { if (!form.key && !form.id) return window.alert('请先填写 API 密钥'); setBusy('fetch-models'); try { const res = form.id && !form.key ? await API.get(`/api/channel/${form.id}/fetch_models`) : await API.post('/api/channel/fetch_models', { type: form.type, key: form.key, base_url: form.base_url }); if (!res.data?.success) throw new Error(res.data?.message || '获取模型失败'); set('models', Array.isArray(res.data.data) ? res.data.data.join(',') : String(res.data.data || '')); } catch (e) { window.alert(e.message || '获取模型失败'); } finally { setBusy(''); } };
  const probeModels = async () => { if (!form.id) return window.alert('请先保存渠道'); setBusy('probe'); try { const res = await API.post(`/api/channel/${form.id}/probe_models`, { models: splitModels(form.models) }); if (!res.data?.success) throw new Error(res.data?.message || '验证失败'); setProbe(res.data.data); } catch (e) { window.alert(e.message || '验证失败'); } finally { setBusy(''); } };
  const loadDefinitions = async () => { try { const res = await API.get('/api/model_definition/aggregated'); if (res.data?.success) setDefinitions(res.data.data || []); else window.alert(res.data?.message || '读取模型配置失败'); } catch (e) { window.alert(e.message || '读取模型配置失败'); } };
  const loadOptions = async () => { try { const res = await API.get('/api/option/'); if (res.data?.success) { const next = {}; (res.data.data || []).forEach((x) => { if (x.key === 'PreConsumedQuota' || x.key === 'DefaultContextLimit') next[x.key] = x.value; }); setOptions(next); } } catch {} };
  useEffect(() => { if (tab === 'models') loadDefinitions(); if (tab === 'basic') loadOptions(); }, [tab]);
  const openModel = (row) => setModelEdit(row ? { ...row, sourceChannelIds: (row.sources || []).map((x) => x.channel_id), model_ratio: row.model_ratio ?? 0, completion_ratio: row.completion_ratio ?? 0 } : { name: '', display_name: '', remark: '', enabled: false, model_type: 'chat', context_limit: 0, support_explicit_cache: false, sourceChannelIds: [], model_ratio: 0, completion_ratio: 0 });
  const saveModel = async (e) => { e.preventDefault(); const m = modelEdit; const body = { name: m.name, display_name: m.display_name || '', remark: m.remark || '', enabled: !!m.enabled, model_type: m.model_type || 'chat', context_limit: Number(m.context_limit) || 0, support_explicit_cache: !!m.support_explicit_cache }; const res = m.id ? await API.put('/api/model_definition/', { ...body, id: m.id }) : await API.post('/api/model_definition/', body); if (!res.data?.success) return window.alert(res.data?.message || '保存模型失败'); const oldSources = new Set((m.sources || []).map((x) => x.channel_id)); const sources = new Set((m.sourceChannelIds || []).map(Number)); for (const channelId of [...sources].filter((x) => !oldSources.has(x))) await API.post('/api/model_definition/source', { model: m.name, channel_id: channelId, group: 'default' }); for (const channelId of [...oldSources].filter((x) => !sources.has(x))) await API.delete('/api/model_definition/source', { data: { model: m.name, channel_id: channelId, group: 'default' } }); const optRes = await API.get('/api/option/'); if (optRes.data?.success) { const map = {}; optRes.data.data.forEach((x) => { map[x.key] = x.value; }); for (const [key, value] of [['ModelRatio', m.model_ratio], ['CompletionRatio', m.completion_ratio]]) { let valueMap = {}; try { valueMap = map[key] ? JSON.parse(map[key]) : {}; } catch {} valueMap[m.name] = Number(value) || 0; await API.put('/api/option/', { key, value: JSON.stringify(valueMap) }); } } setModelEdit(null); loadDefinitions(); };
  const removeModel = async (m) => { if (m.enabled) return window.alert('请先禁用模型再删除'); if (!window.confirm(`确定删除模型 ${m.name}？`)) return; const res = await API.delete(`/api/model_definition/${m.id}`); if (res.data?.success) loadDefinitions(); else window.alert(res.data?.message || '删除失败'); };
  const toggleModel = async (m) => { const body = { id: m.id, name: m.name, display_name: m.display_name || '', remark: m.remark || '', enabled: !m.enabled, model_type: m.model_type || 'chat', redirect_to: m.redirect_to || '', context_limit: Number(m.context_limit) || 0 }; const res = m.in_model_def ? await API.put('/api/model_definition/', body) : await API.post('/api/model_definition/', body); if (res.data?.success) loadDefinitions(); else window.alert(res.data?.message || '切换状态失败'); };
  const testModel = async (m) => { try { const res = await API.get(`/api/model_definition/test?model=${encodeURIComponent(m.name)}`); window.alert(res.data?.success ? `模型测试完成：${JSON.stringify(res.data.data || [])}` : (res.data?.message || '模型测试失败')); } catch (e) { window.alert(e.message || '模型测试失败'); } };
  const saveOptions = async (e) => { e.preventDefault(); const results = await Promise.all(Object.entries(options).map(([key, value]) => API.put('/api/option/', { key, value: String(value) }))); if (results.every((r) => r.data?.success)) window.alert('基础设置已保存'); else window.alert('部分设置保存失败'); };
  return <div className="zjugis-new-page"><PageHead kicker="MODEL CENTER" title="模型配置" description="管理渠道、模型和默认调用参数。" action={<button className="preview-button primary" onClick={() => open()}>＋ 添加模型渠道</button>} />
    <div className="preview-tabs">{[['channels', '渠道配置'], ['models', '模型配置'], ['basic', '基础设置']].map(([k, v]) => <button key={k} className={tab === k ? 'active' : ''} onClick={() => setTab(k)}>{v}</button>)}</div>
    {tab === 'channels' && <><div className="preview-stat-grid"><div><span>已配置渠道</span><strong>{rows.length}</strong></div><div><span>可用模型</span><strong>{models.length}</strong></div><div><span>当前默认模型</span><strong>{models[0] || '—'}</strong></div></div><section className="preview-surface"><div className="preview-section-head"><h2>模型渠道</h2><button className="preview-button" onClick={list.refresh}>↻ 刷新</button></div><div className="preview-table-wrap"><table className="preview-table"><thead><tr><th>名称</th><th>类型</th><th>模型</th><th>分组</th><th>优先级/权重</th><th>状态</th><th>操作</th></tr></thead><tbody>{rows.map((r) => <tr key={r.id}><td><strong>{r.name}</strong><small>{r.base_url || '未设置地址'}</small></td><td>{channelTypes.find((x) => x[0] === Number(r.type))?.[1] || r.type}</td><td>{splitModels(r.models).slice(0, 3).join('、') || '—'}</td><td>{r.group || r.groups || 'default'}</td><td>{r.priority ?? 0} / {r.weight ?? 1}</td><td><span className={r.status === 1 ? 'tag success' : 'tag'}>{r.status === 1 ? '已启用' : '已禁用'}</span></td><td><button className="link-button" onClick={() => open(r)}>编辑</button><button className="link-button" onClick={() => action(r, 'test')}>测试</button><button className="link-button" onClick={() => action(r, r.status === 1 ? 'disable' : 'enable')}>{r.status === 1 ? '停用' : '启用'}</button><button className="link-button" onClick={() => action(r, 'copy')}>复制</button><button className="link-button danger" onClick={() => action(r, 'delete')}>删除</button></td></tr>)}</tbody></table>{!list.loading && rows.length === 0 && <div className="preview-empty">暂无渠道，请添加模型渠道</div>}</div></section></>}
    {tab === 'models' && <section className="preview-surface"><div className="preview-section-head"><h2>模型定义</h2><div className="form-inline-actions"><button className="preview-button" onClick={loadDefinitions}>↻ 刷新</button><button className="preview-button primary" onClick={() => openModel()}>＋ 添加模型</button></div></div><div className="preview-table-wrap"><table className="preview-table"><thead><tr><th>模型</th><th>显示名称</th><th>类型</th><th>来源渠道</th><th>倍率</th><th>上下文</th><th>状态</th><th>操作</th></tr></thead><tbody>{definitions.map((m) => <tr key={m.name}><td><strong>{m.name}</strong><small>{m.remark || '—'}</small></td><td>{m.display_name || '—'}</td><td>{m.model_type || 'chat'}</td><td>{(m.sources || []).map((x) => x.channel_name || `#${x.channel_id}`).join('、') || '—'}</td><td>{m.model_ratio ?? 0} / {m.completion_ratio ?? 0}</td><td>{m.context_limit || '默认'}</td><td><span className={m.enabled ? 'tag success' : 'tag'}>{m.enabled ? '已启用' : '已禁用'}</span></td><td><button className="link-button" onClick={() => openModel(m)}>编辑</button><button className="link-button" onClick={() => testModel(m)}>测试</button><button className="link-button" onClick={() => toggleModel(m)}>{m.enabled ? '停用' : '启用'}</button><button className="link-button danger" onClick={() => removeModel(m)}>删除</button></td></tr>)}</tbody></table>{definitions.length === 0 && <div className="preview-empty">暂无模型定义，可从渠道中的模型创建</div>}</div></section>}
    {tab === 'basic' && <section className="preview-surface"><div className="preview-section-head"><h2>基础设置</h2></div><form className="zjugis-form basic-form" onSubmit={saveOptions}><div className="form-grid"><Field label="预扣额度（token）" type="number" value={options.PreConsumedQuota || ''} onChange={(e) => setOptions({ ...options, PreConsumedQuota: e.target.value })} /><Field label="默认上下文限制（token）" type="number" value={options.DefaultContextLimit || ''} onChange={(e) => setOptions({ ...options, DefaultContextLimit: e.target.value })} /></div><p className="preview-muted">未匹配到具体模型时使用默认上下文限制；预扣额度会在请求完成后按实际用量多退少补。</p><div className="zjugis-modal-actions"><button className="preview-button primary">保存设置</button></div></form></section>}
    {editing !== null && <Modal wide title={form.id ? '编辑模型渠道' : '添加模型渠道'} onClose={() => setEditing(null)}><form className="zjugis-form" onSubmit={save}><div className="form-grid"><Field label="渠道名称" value={form.name} onChange={(e) => set('name', e.target.value)} required /><SelectField label="渠道类型" value={form.type} onChange={(e) => set('type', Number(e.target.value))}>{channelTypes.map(([v, l]) => <option key={v} value={v}>{l}</option>)}</SelectField><Field label="代理 / Base URL" value={form.base_url || ''} onChange={(e) => set('base_url', e.target.value)} /><Field label="API 密钥" type="password" value={form.key || ''} onChange={(e) => set('key', e.target.value)} placeholder={form.id ? '留空表示保持原密钥' : ''} /></div><label className="zjugis-field full"><span>模型列表（逗号或换行分隔）</span><textarea value={form.models || ''} onChange={(e) => set('models', e.target.value)} rows="4" /><div className="form-inline-actions"><button type="button" className="preview-button" onClick={fetchModels} disabled={!!busy}>自动获取模型</button><button type="button" className="preview-button" onClick={probeModels} disabled={!!busy || !form.id}>逐个验证可用性</button><button type="button" className="preview-button" onClick={() => set('models', '')}>清除模型</button></div>{probe && <small className="preview-muted">验证结果：{probe.ok_count}/{probe.total} 个模型可用</small>}</label><div className="form-grid"><Field label="分组（逗号分隔）" value={form.groups || 'default'} onChange={(e) => set('groups', e.target.value)} /><Field label="组织 ID" value={form.openai_organization || ''} onChange={(e) => set('openai_organization', e.target.value)} /><Field label="优先级" type="number" value={form.priority ?? 0} onChange={(e) => set('priority', Number(e.target.value))} /><Field label="权重" type="number" value={form.weight ?? 1} onChange={(e) => set('weight', Number(e.target.value))} /><Field label="测试模型" value={form.test_model || ''} onChange={(e) => set('test_model', e.target.value)} /><SelectField label="自动禁用" value={Number(form.auto_ban) ? 1 : 0} onChange={(e) => set('auto_ban', Number(e.target.value))}><option value="1">开启</option><option value="0">关闭</option></SelectField></div><label className="zjugis-field full"><span>模型映射 JSON</span><textarea value={form.model_mapping || ''} onChange={(e) => set('model_mapping', e.target.value)} rows="3" placeholder='{"qwen3.6-plus":"qwen3.6-plus"}' /></label><label className="zjugis-field full"><span>系统提示词</span><textarea value={form.system_prompt || ''} onChange={(e) => set('system_prompt', e.target.value)} rows="3" /></label><label className="zjugis-field full"><span>其他参数 JSON</span><textarea value={form.other || ''} onChange={(e) => set('other', e.target.value)} rows="3" /></label><div className="zjugis-modal-actions"><button type="button" className="preview-button" onClick={() => setEditing(null)}>取消</button><button className="preview-button primary">保存渠道</button></div></form></Modal>}{modelEdit && <Modal wide title={modelEdit.id ? '编辑模型定义' : '添加模型定义'} onClose={() => setModelEdit(null)}><form className="zjugis-form" onSubmit={saveModel}><div className="form-grid"><Field label="模型 ID" value={modelEdit.name || ''} disabled={!!modelEdit.id} onChange={(e) => setModelEdit({ ...modelEdit, name: e.target.value })} required /><Field label="显示名称" value={modelEdit.display_name || ''} onChange={(e) => setModelEdit({ ...modelEdit, display_name: e.target.value })} /><SelectField label="模型类型" value={modelEdit.model_type || 'chat'} onChange={(e) => setModelEdit({ ...modelEdit, model_type: e.target.value })}><option value="chat">chat</option><option value="image">image</option><option value="audio">audio</option><option value="embedding">embedding</option></SelectField><Field label="上下文限制" type="number" value={modelEdit.context_limit || 0} onChange={(e) => setModelEdit({ ...modelEdit, context_limit: Number(e.target.value) })} /><Field label="输入倍率" type="number" value={modelEdit.model_ratio || 0} onChange={(e) => setModelEdit({ ...modelEdit, model_ratio: Number(e.target.value) })} /><Field label="补全倍率" type="number" value={modelEdit.completion_ratio || 0} onChange={(e) => setModelEdit({ ...modelEdit, completion_ratio: Number(e.target.value) })} /></div><label className="zjugis-field full"><span>备注</span><textarea value={modelEdit.remark || ''} onChange={(e) => setModelEdit({ ...modelEdit, remark: e.target.value })} rows="2" /></label><label className="zjugis-field full"><span>来源渠道</span><select multiple value={(modelEdit.sourceChannelIds || []).map(String)} onChange={(e) => setModelEdit({ ...modelEdit, sourceChannelIds: [...e.target.selectedOptions].map((x) => Number(x.value)) })}>{rows.map((r) => <option key={r.id} value={r.id}>{r.name}（{r.id}）</option>)}</select></label><div className="form-inline-actions"><label className="zjugis-check"><input type="checkbox" checked={!!modelEdit.enabled} onChange={(e) => setModelEdit({ ...modelEdit, enabled: e.target.checked })} />启用模型</label><label className="zjugis-check"><input type="checkbox" checked={!!modelEdit.support_explicit_cache} onChange={(e) => setModelEdit({ ...modelEdit, support_explicit_cache: e.target.checked })} />支持显式缓存</label></div><div className="zjugis-modal-actions"><button type="button" className="preview-button" onClick={() => setModelEdit(null)}>取消</button><button className="preview-button primary">保存模型</button></div></form></Modal>}
  </div>;
}

export function UsersPage() {
  const [keyword, setKeyword] = useState(''); const list = useList('/api/user/', 'p=0&size=100&order=');
  const [modal, setModal] = useState(null); const [detail, setDetail] = useState(null); const [edit, setEdit] = useState(null); const [tokens, setTokens] = useState([]); const [topup, setTopup] = useState({ quota: '', remark: '', expires_in_days: 0, admin_password: '' }); const [tokenForm, setTokenForm] = useState({ name: '', remain_quota: 500000, unlimited_quota: false, expired_time: -1 });
  const [form, setForm] = useState({ username: '', display_name: '', password: '' }); const shown = list.rows.filter((u) => !keyword || JSON.stringify(u).toLowerCase().includes(keyword.toLowerCase())); const set = (key, value) => setForm((f) => ({ ...f, [key]: value }));
  const save = async (e) => { e.preventDefault(); const res = await API.post('/api/user/', form); if (res.data?.success) { setModal(null); setForm({ username: '', display_name: '', password: '' }); list.refresh(); } else window.alert(res.data?.message || '保存失败'); };
  const manage = async (u, action) => { if (!window.confirm(`确定${action === 'delete' ? '删除' : action === 'hard_delete' ? '彻底删除' : '停用'}用户 ${u.username}？`)) return; const res = await API.post('/api/user/manage', { username: u.username, action }); if (res.data?.success) list.refresh(); else window.alert(res.data?.message || '操作失败'); };
  const openEdit = async (u) => { try { const res = await API.get(`/api/user/${u.id}`); if (!res.data?.success) throw new Error(res.data?.message); setEdit({ ...res.data.data, password: '', admin_password: '' }); } catch (e) { window.alert(e.message || '读取用户信息失败'); } };
  const saveEdit = async (e) => { e.preventDefault(); const res = await API.put('/api/user/', { id: edit.id, username: edit.username, display_name: edit.display_name, password: edit.password, phone: edit.phone, group: edit.group, admin_password: edit.admin_password }); if (res.data?.success) { setEdit(null); list.refresh(); } else window.alert(res.data?.message || '更新失败；如修改敏感信息，请填写管理员密码'); };
  const openDetail = async (u) => { setDetail(u); setTokens([]); try { const res = await API.get(`/api/admin/token/user/${u.id}`); if (res.data?.success) setTokens(res.data.data || []); } catch {} };
  const manageToken = async (token, action) => { const res = action === 'delete' ? await API.delete(`/api/admin/token/user/${detail.id}/${token.id}`) : await API.put(`/api/admin/token/user/${detail.id}?status_only=true`, { id: token.id, status: action === 'enable' ? 1 : 2 }); if (res.data?.success) openDetail(detail); else window.alert(res.data?.message || '令牌操作失败'); };
  const createToken = async (e) => { e.preventDefault(); const res = await API.post(`/api/admin/token/user/${detail.id}`, { ...tokenForm, remain_quota: Number(tokenForm.remain_quota), expired_time: tokenForm.expired_time === '' ? -1 : Number(tokenForm.expired_time) }); if (res.data?.success) { setTokenForm({ name: '', remain_quota: 500000, unlimited_quota: false, expired_time: -1 }); openDetail(detail); } else window.alert(res.data?.message || '创建令牌失败'); };
  const grant = async (e) => { e.preventDefault(); const perUnit = Number(localStorage.getItem('quota_per_unit') || 1000); const res = await API.post('/api/topup', { user_id: edit.id, quota: Math.round(Number(topup.quota || 0) * perUnit), remark: topup.remark, expires_in_days: Number(topup.expires_in_days), admin_password: topup.admin_password }); if (res.data?.success) { setTopup({ quota: '', remark: '', expires_in_days: 0, admin_password: '' }); openEdit(edit); list.refresh(); } else window.alert(res.data?.message || '积分发放失败'); };
  return <div className="zjugis-new-page"><PageHead kicker="ACCOUNT CENTER" title="用户管理" description="管理登录账号、权限、额度、令牌和使用状态。" action={<button className="preview-button primary" onClick={() => setModal('add')}>＋ 添加用户</button>} /><div className="preview-stat-grid"><div><span>用户总数</span><strong>{shown.length}</strong></div><div><span>已启用</span><strong>{shown.filter((u) => u.status === 1).length}</strong></div><div><span>累计调用</span><strong>{shown.reduce((n, u) => n + (Number(u.request_count) || 0), 0)}</strong></div></div><section className="preview-surface"><div className="preview-section-head"><h2>用户列表</h2><div className="form-inline-actions"><input className="preview-search" placeholder="搜索 ID、用户名、显示名称或邮箱" value={keyword} onChange={(e) => setKeyword(e.target.value)} /><button className="preview-button" onClick={list.refresh}>↻ 刷新</button></div></div><div className="preview-table-wrap"><table className="preview-table"><thead><tr><th>ID / 用户</th><th>分组</th><th>账户类型</th><th>角色</th><th>剩余额度</th><th>已用额度</th><th>调用次数</th><th>状态</th><th>操作</th></tr></thead><tbody>{shown.map((u) => <tr key={u.id}><td><strong>{u.account_id || `#${u.id}`}</strong><small>{u.display_name || u.username}{u.email ? ` · ${u.email}` : ''}</small></td><td>{u.group || 'default'}</td><td>{u.account_type === 2 ? '企业' : '个体'}</td><td>{u.role === 100 ? '超级管理员' : u.role === 10 ? '管理员' : '普通用户'}</td><td>{u.subscription_quota !== undefined ? Number(u.subscription_quota) + Number(u.timed_quota_total || 0) : (u.quota ?? '—')}</td><td>{u.used_quota ?? '—'}</td><td>{u.request_count ?? 0}</td><td><span className={u.status === 1 ? 'tag success' : 'tag'}>{u.status === 1 ? '已激活' : u.status === 2 ? '已封禁' : '未知'}</span></td><td><button className="link-button" onClick={() => openDetail(u)}>详情 / 令牌</button><button className="link-button" onClick={() => openEdit(u)}>编辑</button><button className="link-button danger" onClick={() => manage(u, u.status === 1 ? 'disable' : 'enable')}>{u.status === 1 ? '停用' : '启用'}</button><button className="link-button danger" onClick={() => manage(u, 'delete')}>删除</button></td></tr>)}</tbody></table>{!list.loading && shown.length === 0 && <div className="preview-empty">暂无用户</div>}</div></section>{modal && <Modal title="添加用户" onClose={() => setModal(null)}><form className="zjugis-form" onSubmit={save}><Field label="用户名" value={form.username} onChange={(e) => set('username', e.target.value)} required /><Field label="显示名称" value={form.display_name} onChange={(e) => set('display_name', e.target.value)} /><Field label="密码" type="password" value={form.password} onChange={(e) => set('password', e.target.value)} required /><div className="zjugis-modal-actions"><button type="button" className="preview-button" onClick={() => setModal(null)}>取消</button><button className="preview-button primary">创建用户</button></div></form></Modal>}{edit && <Modal wide title={`编辑用户 · ${edit.username}`} onClose={() => setEdit(null)}><form className="zjugis-form" onSubmit={saveEdit}><div className="form-grid"><Field label="用户名" value={edit.username || ''} onChange={(e) => setEdit({ ...edit, username: e.target.value })} /><Field label="显示名称" value={edit.display_name || ''} onChange={(e) => setEdit({ ...edit, display_name: e.target.value })} /><Field label="新密码（留空不改）" type="password" value={edit.password || ''} onChange={(e) => setEdit({ ...edit, password: e.target.value })} /><Field label="手机号" value={edit.phone || ''} onChange={(e) => setEdit({ ...edit, phone: e.target.value })} /><Field label="分组" value={edit.group || 'default'} onChange={(e) => setEdit({ ...edit, group: e.target.value })} /><Field label="管理员密码（修改敏感信息时需要）" type="password" value={edit.admin_password || ''} onChange={(e) => setEdit({ ...edit, admin_password: e.target.value })} /></div><div className="zjugis-modal-actions"><button type="button" className="preview-button" onClick={() => setEdit(null)}>取消</button><button className="preview-button primary">保存用户</button></div></form><form className="zjugis-form topup-form" onSubmit={grant}><h3>积分操作</h3><div className="form-grid"><Field label="发放积分" type="number" value={topup.quota} onChange={(e) => setTopup({ ...topup, quota: e.target.value })} /><SelectField label="有效期" value={topup.expires_in_days} onChange={(e) => setTopup({ ...topup, expires_in_days: Number(e.target.value) })}><option value="0">永久</option><option value="7">7 天</option><option value="30">30 天</option><option value="90">90 天</option></SelectField><Field label="备注" value={topup.remark} onChange={(e) => setTopup({ ...topup, remark: e.target.value })} /><Field label="管理员密码" type="password" value={topup.admin_password} onChange={(e) => setTopup({ ...topup, admin_password: e.target.value })} /></div><div className="zjugis-modal-actions"><button className="preview-button">发放积分</button></div></form></Modal>}{detail && <Modal wide title={`用户详情与令牌 · ${detail.username}`} onClose={() => setDetail(null)}><div className="detail-grid">{Object.entries(detail).filter(([k]) => !['password', 'key'].includes(k)).map(([k, v]) => <div key={k}><span>{k}</span><strong>{typeof v === 'object' ? JSON.stringify(v) : String(v ?? '—')}</strong></div>)}</div><div className="token-panel"><div className="preview-section-head"><h3>令牌（{tokens.length}）</h3></div>{tokens.map((t) => <div className="token-row" key={t.id}><div><strong>{t.name}</strong><small>剩余额度：{t.unlimited_quota ? '无限制' : t.remain_quota} · {t.status === 1 ? '已启用' : '已禁用'}</small></div><div><button className="link-button" onClick={() => navigator.clipboard?.writeText(`sk-${t.key}`)}>复制</button><button className="link-button" onClick={() => manageToken(t, t.status === 1 ? 'disable' : 'enable')}>{t.status === 1 ? '禁用' : '启用'}</button><button className="link-button danger" onClick={() => manageToken(t, 'delete')}>删除</button></div></div>)}<form className="zjugis-form token-form" onSubmit={createToken}><h3>添加令牌</h3><div className="form-grid"><Field label="令牌名称" value={tokenForm.name} onChange={(e) => setTokenForm({ ...tokenForm, name: e.target.value })} required /><Field label="令牌额度" type="number" value={tokenForm.remain_quota} disabled={tokenForm.unlimited_quota} onChange={(e) => setTokenForm({ ...tokenForm, remain_quota: e.target.value })} /><SelectField label="有效期" value={tokenForm.expired_time} onChange={(e) => setTokenForm({ ...tokenForm, expired_time: Number(e.target.value) })}><option value="-1">永不过期</option></SelectField><label className="zjugis-check"><input type="checkbox" checked={tokenForm.unlimited_quota} onChange={(e) => setTokenForm({ ...tokenForm, unlimited_quota: e.target.checked })} />无限额度</label></div><div className="zjugis-modal-actions"><button className="preview-button">添加令牌</button></div></form></div></Modal>}</div>;
}

export function LogsPage() {
  const list = useList('/api/log/', 'p=0&page_size=100'); const [keyword, setKeyword] = useState(''); const [detail, setDetail] = useState(null);
  const rows = list.rows.filter((r) => !keyword || JSON.stringify(r).toLowerCase().includes(keyword.toLowerCase()));
  const okay = (r) => r.timing_status === 'ok' || r.type === 0 || r.type === 2;
  return <div className="zjugis-new-page"><PageHead kicker="MODEL USAGE" title="模型日志" description="查看用户问答、模型调用、Token 消耗和请求状态。" action={<button className="preview-button" onClick={list.refresh}>↻ 刷新</button>} /><section className="preview-surface"><div className="preview-section-head"><h2>调用记录</h2><input className="preview-search" placeholder="搜索用户、模型、令牌或请求 ID" value={keyword} onChange={(e) => setKeyword(e.target.value)} /></div><div className="preview-table-wrap"><table className="preview-table"><thead><tr><th>时间</th><th>渠道 / 用户</th><th>令牌</th><th>模型</th><th>提示 / 补全</th><th>缓存读 / 写</th><th>首响应 / 总耗时</th><th>花费</th><th>状态</th><th>详情</th></tr></thead><tbody>{rows.map((r) => <tr key={r.id}><td>{r.created_at ? new Date(r.created_at * 1000).toLocaleString() : '—'}</td><td><strong>{r.channel_name || r.channel_id || '—'}</strong><small>{r.username || r.user_id || '—'}</small></td><td>{r.token_name || '—'}</td><td>{r.model_name || '—'}</td><td>{r.prompt_tokens || 0} / {r.completion_tokens || 0}</td><td>{r.cache_read_tokens || 0} / {r.cache_write_tokens || 0}</td><td>{r.first_chunk_ms ? `${r.first_chunk_ms} ms` : '—'} / {r.elapsed_time ? `${r.elapsed_time} ms` : '—'}</td><td>{r.quota ?? '—'}</td><td><span className={okay(r) ? 'tag success' : 'tag'}>{okay(r) ? '成功' : (r.timing_status || '异常')}</span></td><td><button className="link-button" onClick={() => setDetail(r)}>查看</button></td></tr>)}</tbody></table>{!list.loading && rows.length === 0 && <div className="preview-empty">暂无调用日志</div>}</div></section>{detail && <Modal wide title={`调用详情 · ${detail.request_id || detail.id}`} onClose={() => setDetail(null)}><div className="detail-grid">{Object.entries(detail).map(([k, v]) => <div key={k}><span>{k}</span><strong>{typeof v === 'object' ? JSON.stringify(v) : String(v ?? '—')}</strong></div>)}</div></Modal>}</div>;
}
export function AccountPage() { const [user, setUser] = useState(null); useEffect(() => { API.get('/api/user/self').then((r) => r.data?.success && setUser(r.data.data)).catch(() => {}); }, []); const logout = async () => { await API.get('/api/user/logout'); localStorage.removeItem('user'); window.location.href = '/login'; }; return <div className="zjugis-new-page"><PageHead kicker="ACCOUNT SETTINGS" title="账户设置" description="管理当前管理员账户和登录安全设置。" /><div className="account-grid"><section className="preview-surface account-card"><div className="preview-card-kicker">PROFILE</div><h2>账户信息</h2><div className="account-row"><span>登录账号</span><strong>{user?.username || '—'}</strong></div><div className="account-row"><span>账户角色</span><strong className="green">{user?.role === 100 ? '超级管理员' : '管理员'}</strong></div><div className="account-row"><span>登录状态</span><strong className="green">已登录</strong></div></section><section className="preview-surface account-card"><div className="preview-card-kicker">SECURITY</div><h2>安全设置</h2><div className="account-row"><span>会话有效期</span><strong>默认</strong></div><div className="account-row"><span>登录保护</span><strong className="green">已启用</strong></div><button className="link-button logout-link" onClick={logout}>退出登录</button></section></div></div>; }
export default ModelConfigPage;
