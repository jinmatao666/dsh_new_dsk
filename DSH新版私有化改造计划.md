# DSH 新版私有化改造计划

## 1. 当前背景

- 现有版本基于旧版 DSH 完成了私有化改造，桌面安装包、OneAPI 服务镜像和数据库已经运行稳定。
- 当前版本继续保存在原仓库的 `main` 分支。
- 新版 DSH 已推送到同一仓库的 `new-version` 分支，与现有版本隔离维护，不进行相互合并。
- 新版目前处于开发和验证阶段，暂定产品名称为“万维 Buddy 预览版”。
- 新版验证通过后，将直接替代现有桌面版本。
- 桌面安装包统一由 GitHub Runner 构建，本地开发环境不制作正式安装包。
- 现有 OneAPI 镜像部署在 4090 服务器上；确需更新时，将交付 tar 上传至 4090，在该服务器构建镜像并替换现有服务。

## 2. 总体目标

本次改造不只是把旧版修改重新合并到新版，而是建立一套可以长期升级的私有化架构：

1. 官方 DSH 作为可替换的基础内核，尽量保持原样。
2. 万维私有化能力通过适配层、业务插件和产品组装层独立实现。
3. 后续 DSH 升级时，优先只调整兼容适配层和少量必要补丁。
4. 继续沿用现有 OneAPI 服务镜像、API 协议和数据库。
5. DSH 客户端升级原则上只需要重新构建并发布桌面安装包。
6. 新旧桌面版本在测试阶段可以同时安装和运行，互不覆盖本地数据。

## 3. 分支和版本管理

```text
同一个远程仓库
├─ main          当前已经运行的旧版本
└─ new-version   基于新版 DSH 的私有化改造版本
```

- 旧版本目录只维护和推送 `main`。
- 新版本目录只维护和推送 `new-version`。
- 两个分支保持隔离，不执行相互合并。
- 新版开发期间发布“预览版”安装包，避免覆盖当前正式版本。

## 4. 目标架构

```text
官方 DSH 新版本
├─ 官方代码
│  └─ 尽量保持原样
│
├─ 万维兼容适配层
│  ├─ 会话适配
│  ├─ 工作区适配
│  ├─ 模型适配
│  └─ OneAPI 通信适配
│
├─ 万维业务插件
│  ├─ 登录和账户
│  ├─ 模型及角色权限
│  ├─ 技能市场
│  ├─ 文档解析
│  ├─ 图片识别
│  └─ 审计上报
│
├─ 万维产品界面
│  ├─ 品牌
│  ├─ 中文文案
│  ├─ 文件拖入
│  ├─ 交付物展示
│  └─ 设置页面
│
└─ 桌面交付层
   ├─ Tauri 桌面壳
   ├─ DSH Sidecar
   ├─ wanwei-desktop profile
   └─ Windows/macOS/Linux 安装包
```

### 4.1 官方层

官方源码作为升级基础，避免直接把业务逻辑写入官方核心包。

### 4.2 兼容适配层

适配层负责屏蔽不同 DSH 版本之间的接口变化。万维业务代码只依赖自有的稳定接口，不直接依赖容易变化的 DSH 内部实现。

建议逐步建立以下接口：

```ts
interface WanweiSessionApi {}
interface WanweiWorkspaceApi {}
interface WanweiModelApi {}
interface WanweiSkillApi {}
```

### 4.3 业务插件层

登录、模型权限、技能市场、文档解析和视觉能力等功能分别作为独立插件维护，优先通过 DSH 的 service、event、slot 和 profile overlay 接入。

### 4.4 产品组装层

通过独立的 `wanwei-desktop` bundle/profile 组装官方能力和万维私有插件，不在组装层放置具体业务实现。

### 4.5 交付层

桌面安装包、Sidecar、离线运行时、升级器和 CI/CD 独立维护，只消费已经组装完成的产品。开发机只验证源码、运行时暂存和桌面二进制；Windows、macOS 和 Linux 安装包由 GitHub Runner 统一构建。

## 5. OneAPI 和数据库策略

当前没有独立的 DSH 服务端镜像。DSH CLI、Web 前端和本地插件随桌面安装包交付，线上仅保留 OneAPI 服务镜像。

因此正常的 DSH 升级流程应当是：

```text
更新官方 DSH
→ 调整万维兼容适配层
→ 验证现有 OneAPI API
→ 推送代码并由 GitHub Runner 构建桌面安装包
→ OneAPI 镜像和数据库继续沿用
```

只有登录协议、模型权限、技能格式、审计或其他服务端能力发生变化时，才更新 OneAPI 镜像。更新方式固定为：准备服务端交付 tar，上传到 4090 服务器，在 4090 上构建新镜像并替换现有服务；桌面端 DSH 升级不能自动触发这一步。

即使 OneAPI 镜像需要升级，也继续挂载并使用现有数据库和持久化目录，只允许执行受控的增量数据库迁移，禁止清空或重建业务数据。

需要保持稳定的主要接口包括：

- `/api/user/login`
- `/api/token/`
- `/api/user/available_models/detail`
- `/api/status`
- `/api/client-event`
- `/api/skill/...`
- `/v1/models`
- `/v1/chat/completions`

后续建议增加明确的 OneAPI 客户端协议版本和能力声明，由桌面端启动时检查兼容性。

## 6. 改造阶段

### 第一阶段：打通最小运行链路

目标是验证新版桌面端可以继续使用现有 OneAPI 服务和数据库。

当前进度（2026-09-16）：

- [x] 建立独立的 `wanwei-desktop` bundle/profile，不在官方 Web bundle 中承载私有逻辑。
- [x] 建立 `products/wanwei-desktop` 私有桌面交付目录和最小 Tauri 2 桌面壳。
- [x] 使用 `com.wanwei.harness.preview` 隔离预览版安装身份，使用 `com.wanwei.harness.preview.development` 继续隔离本地开发版。
- [x] 将旧桌面壳的最小 Sidecar 生命周期迁移到新产品目录，并启动 `wanwei-desktop` profile。
- [x] 将预览版 `DSH_HOME` 固定在自身平台应用数据目录下，不读取旧版用户级默认目录。
- [x] 为安装包实现自包含 Node + DSH 运行时暂存。
- [x] 使用暂存目录中的 Node 和生产 CLI 完成 `wanwei-desktop` 真实启动与鉴权交换。
- [x] 完成包含自包含运行时的 Tauri release 二进制构建验证。
- [x] 将本地 release 二进制验证与 GitHub Runner 安装包构建命令分离，并禁止本地执行 Runner 打包入口。
- [x] 将旧版 OneAPI Host 登录、Token、模型同步、受管 Provider 和默认模型逻辑迁入独立万维包，并通过模拟现有协议的链路测试。
- [x] 迁移旧版 `ui-oneapi-auth` 的完整品牌登录界面、真实账号登录、状态恢复和账户页，适配新版 DSH 的服务、事件、插槽、Locale 和模型接口；短信/扫码保留紫色预览入口并明确标记为暂未开放，不模拟成功。
- [x] 复用旧版 desktop bundle 的 OneAPI Provider 组装与登录门禁，并按新版 Slot、Locale 和客户端构建机制完成迁移。
- [x] 使用现有线上 OneAPI 地址和真实账号完成新版桌面开发壳登录验证，登录后可正常进入新版主界面。
- [x] 复用旧版 `release.yml` 桌面构建思路，新增手动触发的 Windows x64、macOS arm64 与 Linux x64 Runner workflow，分别生成 NSIS、DMG、DEB/AppImage 产物，并与镜像/数据库部署完全隔离。
- [x] 接通现有 OneAPI 登录、模型列表和首轮会话；已使用真实账号和授权模型 `qwen3.8-27b-fp8` 完成首轮回复与用量统计验证。

验收内容：

- 新版 DSH 可以正常构建和启动。
- 恢复 Tauri 桌面壳。
- 建立 `wanwei-desktop` profile。
- 新旧桌面版本可以同时安装和运行。
- 新版桌面端可以连接现有 OneAPI。
- 可以使用现有账号登录。
- 可以拉取当前账号允许使用的模型。
- 可以创建会话并完成一次模型对话。
- 不要求修改现有 OneAPI 镜像或数据库。

第一阶段暂不迁移技能市场和复杂界面定制。

### 第二阶段：迁移业务能力

- 角色和模型权限。
- 技能市场。
- 技能安装、刷新和分类同步。
- 文档解析。
- 图片识别。
- 文件附件和外部文件拖入。
- 会话日志下载。
- 客户端审计事件。

### 第三阶段：迁移产品体验

- 万维 Buddy 品牌和图标。
- 中文界面和中文提示。
- 设置页面和账户入口。
- 侧栏和工作区交互。
- 工具结果、表格和空间分析结果展示。
- 交付文件展示和下载。
- 桌面托盘、窗口状态和升级体验。

### 第四阶段：发布和替换

- Windows 安装包。
- macOS 安装包。
- Linux 安装包。
- 自动升级。
- 新旧客户端本地数据迁移。
- CI/CD 和发布通道。
- 预览版验收。
- 正式替换旧桌面版本。

## 7. 代码修改规则

1. 不把旧版官方目录整体覆盖到新版。
2. 不使用旧版 `pnpm-lock.yaml` 覆盖新版锁文件。
3. 新增业务能力优先放入万维自有包或目录。
4. 产品组装放入独立的 `wanwei-desktop` bundle/profile。
5. OneAPI 源码可以保留在 `services/one-api`，但 DSH 升级不应触发 OneAPI 镜像重建。
6. OneAPI 镜像只在 `services/one-api` 发生变化并经过确认时更新：交付 tar 上传至 4090，在 4090 上构建并替换。
7. 桌面安装包只由 GitHub Runner 构建，本地不产出正式安装包。
8. 确实需要修改官方文件时，必须作为独立的“官方兼容补丁”提交。
9. 业务功能提交和官方兼容补丁不能混在同一个提交中。
10. 每项 OneAPI 相关能力都应增加针对现有服务协议的接口测试。
11. 优先复用已经验证的业务行为、接口、配置和素材，不强求逐行复用旧代码。

## 8. 新旧版本隔离

预览版与当前正式版至少隔离以下内容：

- 应用显示名称。
- Windows/macOS/Linux 应用标识。
- 安装目录。
- 安装器 Upgrade Code。
- 本地配置目录。
- 缓存目录。
- 日志目录。
- 会话和工作区元数据目录。
- 自动更新通道。
- 安装包文件名和发布标签。

当前确定的桌面应用标识为：

```text
现有正式版   com.wanwei.harness
安装预览版   com.wanwei.harness.preview
本地开发版   com.wanwei.harness.preview.development
```

Tauri 会根据应用标识隔离单实例锁、WebView profile 和平台应用数据目录。后续接入 Sidecar 时，`DSH_HOME` 仍需显式指向预览版应用数据目录下的独立子目录，不能回退到用户级默认目录。

两者可以调用同一套 OneAPI API。测试阶段优先使用专门的测试账号，避免测试操作污染正式用户和审计数据。

## 9. 第一阶段完成标准

第一阶段完成时，应当能够证明：

```text
万维 Buddy 预览版
→ 新版 DSH 本地运行时
→ 现有 OneAPI 服务
→ 现有模型服务
→ 完成一次正常会话
```

同时满足：

- 旧版仍可正常运行。
- OneAPI 服务镜像不变。
- OneAPI 数据库不变。
- 旧版安装目录和本地数据不受影响。
- 新版改造已经按照适配层、业务插件和产品组装层进行分离。

## 10. 待继续讨论的问题

- 首个预览安装包支持 Windows x64、macOS arm64 和 Linux x64；是否追加 macOS Intel、Windows arm64 或 Linux arm64 留待真实需求确认。
- 新旧桌面端是否允许同时登录同一账号。
- 新版是否读取旧版的本地会话和工作区数据。
- OneAPI 客户端协议版本和能力声明的具体字段。
- 万维自有包的目录结构和 npm 命名空间。
- 第一阶段是否保留旧版全部权限模式和审批流程。
- 预览版通过后的正式替换和回退方式。
