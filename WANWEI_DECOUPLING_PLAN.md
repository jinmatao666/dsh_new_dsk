# 万维产品层解耦计划

## 目标

官方 DSH 作为可替换底座，只保留不含万维语义的稳定扩展点。万维认证、模型治理、桌面能力、业务协议、品牌、主题和安装组装全部由 `packages/wanwei/*`、`packages/bundle/wanwei-desktop` 与 `products/wanwei-desktop` 拥有。

## 验收标准

1. 官方代码不出现 `Wanwei`、`ZJUGIS`、`OneAPI`、`WANWEI_RESULT` 等私有标识。
2. 删除万维包、产品目录和万维 Bundle 后，纯官方组合仍可安装依赖、类型检查和构建。
3. 万维功能只通过公开 Slot、Service、Controller 或 Remote 接入，不读取官方组件内部状态。
4. 官方层新增的接口必须通用、可独立测试，并在没有实现者时保持原有官方行为。
5. OneAPI 响应和万维成果协议先在万维层转换成通用领域数据，再交给官方 UI。
6. 官方补丁形成短小、可追踪的清单；下次升级只复核这些扩展点和万维适配层。
7. CI 分别验证纯官方组合和万维桌面组合。

## 分阶段实施

### 第一阶段：桌面桥依赖反转

- 建立不含产品名称的客户端桌面能力 Service。
- 官方 Workspace 和 Session 导出只消费通用能力。
- 万维产品层负责把 Tauri 命令桥注册为能力实现。
- 清除官方包中的 `__ZJUGIS_NATIVE_INVOKE__` 和 Tauri 全局探测。
- 补充无 Provider 与有 Provider 两条路径测试。

### 第二阶段：业务能力下沉

- 将 OneAPI 视觉识别从 `packages/host/vision` 移入 `packages/wanwei/*`。
- 将私有技能市场实现迁入万维包；官方技能 UI 只保留通用目录/安装接口。
- 将 `WANWEI_RESULT` 解析和空间分析成果卡移入万维 Presenter。
- 将文件导入、拖放和目录打开实现统一收口到万维桌面集成包。

### 第三阶段：页面与样式隔离

- 官方 CSS 恢复上游默认值。
- 万维品牌、颜色、间距和产品文案由产品主题变量、品牌 Slot 和包装组件提供。
- 静态品牌及业务图标迁至万维产品资源目录，Runner 在组装阶段复制。

### 第四阶段：升级保护

- 新增私有标识扫描，阻止私有名称进入官方目录。
- 新增纯官方组合构建检查和万维组合测试。
- 生成官方补丁清单，记录每个扩展点、原因、所有者及覆盖测试。
- 在升级文档中固定“更新上游、复核扩展点、更新适配器、双组合验证、Runner 打包”的流程。

## 官方补丁预算

允许保留的官方改动仅限：通用 Slot/Service 定义、对应的无实现降级路径、测试以及必要的构建入口。任何产品名称、服务地址、业务中文文案、Tauri 命令名和私有数据协议均不得进入官方包。

## 当前执行顺序

1. 桌面桥依赖反转。
2. Vision/OneAPI 下沉。
3. 成果 Presenter 下沉。
4. 技能市场与文件导入收口。
5. 样式和资源隔离。
6. 双组合 CI 与私有标识门禁。

## 当前进度

- [x] 桌面桥依赖反转。
- [x] OneAPI Vision 下沉到万维包。
- [x] 成果协议与分析 Presenter 下沉到万维包。
- [x] 私有技能市场下沉到万维包。
- [x] 文件导入、拖放提示与品牌资源收口到万维产品层。
- [x] 恢复官方 Sidebar、Settings、附件、技能和问答默认样式与文案。
- [x] 增加私有标识和官方静态资源目录 CI 门禁。
- [x] 增加独立的纯官方组合构建任务。
- [ ] 在下一次真实上游升级中验证补丁清单。
